Original media masters live in this directory.

- `images/masters/` stores the source JPG assets.
- `videos/masters/` stores the source MP4 assets.

The files inside `public/` are the optimized production derivatives used by the site.
